<Window x:Class="KnizhnyMir.Desktop.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Книжный мир — Текущий заказ" Height="520" Width="760"
        MinHeight="400" MinWidth="600"
        Icon="/Resources/icon.ico"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="14">
        <TextBlock DockPanel.Dock="Top" Text="Текущий заказ" FontSize="20" FontWeight="Bold"
                   Foreground="#FF4A3000" Margin="0,0,0,8" />
        <TextBlock DockPanel.Dock="Top" x:Name="ClientText" Margin="0,0,0,10" FontWeight="SemiBold" />

        <Border DockPanel.Dock="Bottom" Margin="0,10,0,0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="Оформить заказ" Click="PlaceOrder_Click" FontWeight="Bold" />
                    <Button Content="Закрыть" Click="Close_Click" Background="#FF9E9E9E" />
                </StackPanel>
                <TextBlock x:Name="TotalText" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"
                           Foreground="#FF4A3000" />
            </DockPanel>
        </Border>

        <Grid>
            <DataGrid x:Name="CartGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowHeight="34" FontSize="14" SelectionMode="Single"
                      CellEditEnding="CartGrid_CellEditEnding"
                      Background="White" RowBackground="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Товар" Binding="{Binding Product.Name}"
                                        IsReadOnly="True" Width="*" />
                    <DataGridTextColumn Header="Цена, ₽" Binding="{Binding Product.Price, Mode=OneWay, StringFormat=N2}"
                                        IsReadOnly="True" Width="100" />
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity, Mode=TwoWay}" Width="100" />
                    <DataGridTextColumn Header="Сумма, ₽" Binding="{Binding Sum, Mode=OneWay, StringFormat=N2}"
                                        IsReadOnly="True" Width="110" />
                    <DataGridTemplateColumn Header="" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" Click="Remove_Click" CommandParameter="{Binding}"
                                        Padding="6,2" FontSize="12" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="EmptyText" Text="Заказ пуст. Добавьте товары из каталога."
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="16" Foreground="#FF999999" Visibility="Collapsed" />
        </Grid>
    </DockPanel>
</Window>
